# FastAPI layer (backend/) — adds the HTTP stack on top of the dashboard deps.
-r requirements.txt
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
# SaaS layer: auth (JWT + bcrypt) and Stripe billing.
PyJWT>=2.8.0
bcrypt>=4.0.0
stripe>=11.0.0
